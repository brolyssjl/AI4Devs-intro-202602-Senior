Act as Senior Frontend engineer to develop an small application in HTML and Javascript to show a UI where the user can use an input to fulfil with whatever string and the application needs to reverse the string.

```markdown
Example: if I input `AI4Devs`, it should return `sveD4IA`.
```

The UI should be friendly and responsive to visualise the UI components in any screen size, you can use the image attached as guide. Use an existing library to reduce the time of creating custom style sheets; for example check: https://materializecss.com/getting-started.html.
Keep in mind that the image is a reference because we want to improve the manual action (using a button) for the reverse action that can happen in real time which means, that every time the user write a character the app shows the result immediately.

Generate the files separately, which means: HTML, Javascript and CSS files if needed.

Notas:

1. En el prompt agregué la imagen de la carpeta res/reverstring-example.png como guia.
2. El resultado fue perfecto excepto porque estaba renderizando el tema oscuro, y el input field no se veia bien, pero solamente tuve que decirle: `The colors are dark and the input is not correct. Fix it` y le pase el screenshot para que pudiera ver el problema visual.
